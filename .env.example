# GotR Voice Worker — copy to .env and fill in values

# Enable dev-only studio routes. Set FALSE in production.
DEV=TRUE

# Set to false to skip torch/chatterbox imports (production web server, no GPU needed).
# Set to true on the local GPU machine running the TTS worker.
GENERATION_ENABLED=true
